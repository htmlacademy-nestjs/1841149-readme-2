export enum EnvAppValidationMessage {
  EnvironmentRequired = 'Environment is required',
  EnvironmentType = 'Environment type should be one of development, stage or production',
  PortRequired = 'Port is required',
}
